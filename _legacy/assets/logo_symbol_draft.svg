<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 500 500">
  <defs>
    <style>
      .cls-1 { fill: #222831; }
      .cls-2 { fill: none; stroke-miterlimit: 10; stroke-width: 14px; }
      .peach { stroke: #DEA07C; }
      .grey { stroke: #A0A0A0; }
      .white { stroke: #FFFFFF; }
    </style>
  </defs>
  
  <!-- Background -->
  <rect class="cls-1" width="500" height="500"/>
  
  <!-- Content Group centered -->
  <g transform="translate(50, 50)">
      
      <!-- PEACH SHAPE (Left) -->
      <!-- A hairpin shape with a circle inside the bottom curve -->
      <!-- Path: Up left side, sharp turn, down right side, arc at bottom -->
      <path class="cls-2 peach" d="M 120,320 L 190,120 L 220,205" stroke-linecap="round" stroke-linejoin="round" />
      <path class="cls-2 peach" d="M 120,320 A 25,25 0 1,0 165,335 L 190,120" stroke-linecap="round" stroke-linejoin="round" />
      <!-- Wait, the path needs to be one continuous loop or carefully aligned parts -->
      <!-- Let's redraw Peach as a single path + circle -->
      <!-- Left leg up, turn, right leg down, arc -->
      <path class="cls-2 peach" d="M 130,320 L 180,120 L 230,260" stroke-linecap="round" stroke-linejoin="round" opacity="0" /> <!-- Reference -->
      
      <!-- Actual Peach Geometry -->
      <!-- Inner Circle -->
      <circle cx="130" cy="320" r="15" class="cls-2 peach" />
      <!-- Outer Hairpin -->
      <path class="cls-2 peach" d="M 155,320 L 205,120 L 105,320 A 25,25 0 0,0 155,320" stroke-linecap="round" stroke-linejoin="round" fill="none"/>
      <!-- Correction: The peach shape is a slanted loop. Let's adjust coordinates -->
      <!-- Top Peak: (180, 100). Bottom Left Center: (120, 300) -->
      <!-- Vector: (60, -200) -->
      <!-- Let's draw the V shape first -->
      <!-- Right Leg: (180,100) -> (145, 300) -->
      <!-- Left Leg: (180,100) -> (95, 300) -->
      <!-- Arc connecting (95,300) and (145,300) at bottom -->
      <path class="cls-2 peach" d="M 145,300 L 180,100 L 95,300 A 25,25 0 1,0 145,300 Z" stroke-linejoin="round" />
      <circle cx="120" cy="300" r="10" fill="#DEA07C" stroke="none" /> 
      
      <!-- GREY SHAPE (Middle) -->
      <!-- Just a Chevron, behind others? -->
      <!-- Peak: (230, 100). Legs parallel to Peach -->
      <!-- Left Leg: (230,100) -> (195, 300) -->
      <!-- Right Leg: (230,100) -> (265, 300) -->
      <path class="cls-2 grey" d="M 195,300 L 230,100 L 265,300" stroke-linecap="round" stroke-linejoin="round" />
      
      <!-- WHITE SHAPE (Right) -->
      <!-- Triangle with loop at bottom right -->
      <!-- Peak: (280, 100) -->
      <!-- Left Leg: (280,100) -> (230, 300) -->
      <!-- Right Leg: (280,100) -> (330, 300) -->
      <!-- Base: (230,300) -> (330,300) -->
      <!-- But with a loop at (330,300) -->
      <path class="cls-2 white" d="M 230,300 L 280,100 L 330,300" stroke-linecap="round" stroke-linejoin="round" />
      <path class="cls-2 white" d="M 230,300 L 315,300" stroke-linecap="round" stroke-linejoin="round" />
      <!-- Loop -->
      <circle cx="330" cy="300" r="15" class="cls-2 white" />
      <!-- Connect loop to triangle? -->
      
  </g>
</svg>